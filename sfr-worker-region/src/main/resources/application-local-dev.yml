spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/db-worker-region
    username: user
    password: password

  kafka:
    bootstrap-servers: localhost:9094
    consumer:
      properties:
        schema.registry.url: http://localhost:8085